import 'package:flutter/material.dart';

class Currencyapikey extends StatelessWidget {
  const Currencyapikey({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
